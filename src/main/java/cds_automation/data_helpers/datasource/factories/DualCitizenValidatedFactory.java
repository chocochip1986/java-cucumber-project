package cds_automation.data_helpers.datasource.factories;

import cds_automation.models.datasource.Batch;
import cds_automation.models.datasource.DualCitizenValidated;
import org.springframework.stereotype.Component;

@Component
public class DualCitizenValidatedFactory extends AbstractFactory {
    public DualCitizenValidated create(Batch batch) {
        return create(batch);
    }
}
